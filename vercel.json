{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build",
  "regions": ["fra1"],
  "cleanUrls": true,
  "github": {
    "silent": true
  }
  /* Eliminamos la redirección para que Next.js utilice la página index por defecto */
}